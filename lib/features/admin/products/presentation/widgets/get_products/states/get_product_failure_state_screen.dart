import 'package:flutter/material.dart';

class GetProductFailureStateScreen extends StatelessWidget {
  const GetProductFailureStateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Error'));
  }
}
